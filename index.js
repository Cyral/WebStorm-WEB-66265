import styles from './styles.css';

// Ctrl+Click (or Right Click > Go To > Declaration) WORKS!
console.log(styles.first);

// Ctrl+Click does not work ("Cannot find declaration to go to")
console.log(styles.second)
